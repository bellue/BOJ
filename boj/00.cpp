#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

vector<vector<int>> v;
vector<int> v1(5,1);

int main() {
   ios::sync_with_stdio(false); cin.tie(nullptr);

    int m = 2;
    if(m&(1<<1)) cout << "true";
}